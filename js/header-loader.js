(async function () { const mount = document.getElementById("header"); if (!mount) return; const isSubfolder = window.location.pathname.split("/").length > 2; const prefix = isSubfolder ? "../" : ""; const getCurrentMetal = () => { const path = window.location.pathname.replace(/\\/g, "/").toLowerCase(); const params = new URLSearchParams(window.location.search); const category = (params.get("metal") || params.get("type") || "").toLowerCase(); if (path.includes("silver") || category.includes("silver")) return "silver"; if (path.includes("coming-soon") || path.includes("platinum") || category.includes("platinum")) return "platinum"; if (path.includes("diamond") || category.includes("diamond")) return "diamond"; return "gold"; }; const applyInitialRateState = (html) => { const currentMetal = getCurrentMetal(); const shouldHideRate = currentMetal === "diamond"; document.documentElement.classList.toggle("kj-hide-rate-select", shouldHideRate); document.body?.classList.toggle("kj-hide-rate-select", shouldHideRate); const template = document.createElement("template"); template.innerHTML = html; const dropdown = template.content.querySelector(".rate-select"); if (!dropdown) return html; if (shouldHideRate) { dropdown.hidden = true; dropdown.classList.add("rate--hidden"); return template.innerHTML; } const menu = dropdown.querySelector(".rate-list"); if (menu) { const rows = Array.from(menu.querySelectorAll(".rate-option")); if (rows.length > 0) { let primaryRow = rows.find(r => r.dataset.metal === currentMetal); if (!primaryRow) { primaryRow = rows.find(r => r.dataset.metal === "gold") || rows[0]; } const triggerText = dropdown.querySelector(".rate-btn span"); const triggerImage = dropdown.querySelector(".rate-btn img"); dropdown.setAttribute("data-kj-rate-order", currentMetal); if (triggerText) { const primaryLabelSpan = primaryRow.querySelector("[data-rate-label]"); triggerText.textContent = primaryLabelSpan ? primaryLabelSpan.textContent : ""; triggerText.setAttribute("data-rate-type", primaryRow.dataset.rateType); } if (triggerImage) { const primaryImg = primaryRow.querySelector("img"); if (primaryImg) { triggerImage.setAttribute("src", prefix + primaryImg.getAttribute("src")); } } if (currentMetal !== "diamond") { const otherRows = rows.filter(r => r !== primaryRow); menu.innerHTML = ""; menu.appendChild(primaryRow); otherRows.forEach(r => menu.appendChild(r)); } } } return template.innerHTML; }; try { const response = await fetch(prefix + "partials/header.html?v=27"); if (!response.ok) { throw new Error("Header fetch failed: " + response.status); } let html = await response.text(); if (prefix) { html = html.replace(/(href|src)="(?!https?:|mailto:|tel:|#|\/|\.\.\/)([^"]+)"/g, `$1="${prefix}$2"`); html = html.replace(/srcset="([^"]+)"/g, (_, contents) => { return 'srcset="' + contents.split(',').map(part => { part = part.trim(); return (part && !part.startsWith('http') && !part.startsWith('/') && !part.startsWith('../')) ? prefix + part : part; }).join(', ') + '"'; }); } html = applyInitialRateState(html); mount.innerHTML = html; window.dispatchEvent(new CustomEvent("kerala:header-loaded")); } catch (error) { console.error("Header loader error:", error); } })();